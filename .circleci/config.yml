version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Test
          command: perl test.pl
      - run: apt-get update -y && apt-get install openssh-client git

